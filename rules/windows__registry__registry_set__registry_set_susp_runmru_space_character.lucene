(registry.path:*\\Software\\Microsoft\\Windows\\CurrentVersion\\Explorer\\RunMRU\* AND winlog.event_data.Details:*#*) AND (winlog.event_data.Details:(*            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *\ \ \ \ \ \ \ \ \ \ \ \ *))
