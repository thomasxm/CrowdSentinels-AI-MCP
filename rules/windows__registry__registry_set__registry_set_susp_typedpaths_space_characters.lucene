(registry.path:*\\Software\\Microsoft\\Windows\\CurrentVersion\\Explorer\\TypedPaths\\url1 AND winlog.event_data.Details:*#*) AND (winlog.event_data.Details:(*            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *            * OR *\ \ \ \ \ \ \ \ \ \ \ \ *))
