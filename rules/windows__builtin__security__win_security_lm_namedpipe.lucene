winlog.channel:Security AND ((event.code:5145 AND winlog.event_data.ShareName:\\\*\\IPC$) AND (NOT (winlog.event_data.RelativeTargetName:(atsvc OR samr OR lsarpc OR lsass OR winreg OR netlogon OR srvsvc OR protected_storage OR wkssvc OR browser OR netdfs OR svcctl OR spoolss OR ntsvcs OR LSM_API_service OR HydraLsPipe OR TermSrv_API_service OR MsFteWds OR sql\\query OR eventlog))))
